# ЛР #2: [C++ & UNIX]: C++ BUILD / IF / LOOP, PYTHON

Студент: Воропаев И. С.

Группа: Z4100

Преподаватель: Маслов И. Д.

## Цель

Познакомить студента с принципами компиляции исходного кода. Составить
программу с использованием циклов, условий и функций. Сравнить быстродействие
между C++ и Python. Ознакомление с типами данных.

## Задание 1

**[С++ EXPRESSION] Создать и скомпилировать программу на C++**

Результат сборки (компиляции) сохранять в папку build. Папку build сделать
игнорируемой для GIT. Программа должна получать на вход число – это
количество итераций для выполнения расчета. В рамках итерации выполнять
следующее вычисление: `x^2 - x^2 + x*4 - x*5 + x + x`. Вычисление выполнять в виде
отдельной от `main` функции, которая будет вызвана циклически из `main`.
Фиксировать время выполнения программы, затрачиваемое на расчет выражения
`n` раз (`n` задается в консоли перед вычислением). Предусмотреть дополнительный
цикл на повторную итерацию запуска программы вычислений. Если было введено
не число, то завершить выполнение программы.

Реализованная программа находится в файле [`lab2/src/main.cpp`](../src/main.cpp)

Компиляция и запуск программы делалась следующими командами (`.exe` файл помещается в папку `build`):

```
g++ -o lab2/build/main.exe lab2/src/main.cpp
lab2/build/main.exe
```
В программе есть функция - `measure_time(x, num_loops)`, которая выполняет необходимы вычисления и измеряет время выполнения. Она вызывается из функции `main`.

## Задание 2

**[PYTHON EXPRESSION] Создать и скомпилировать программу на Python 3**

Результат сборки (компиляции) сохранять в папку `build`. Папку `build` сделать
игнорируемой для GIT. Программа должна получать на вход число – это
количество итераций для выполнения расчета. В рамках итерации выполнять
следующее вычисление: `x^2 - x^2 + x*4 - x*5 + x + x`. Вычисление выполнять в виде
отдельной от `main` функции, которая будет вызвана циклически из `main`.
Фиксировать время выполнения программы, затрачиваемое на расчет выражения
`n` раз (`n` задается в консоли перед вычислением). Предусмотреть дополнительный
цикл на повторную итерацию запуска программы вычислений. Если было введено
не число, то завершить выполнение программы.

Реализованная программа находится в файле [`lab2/src/py_main.py`](../src/py_main.py)

Запуск программы:

```
python3 lab2/src/py_main.py
```

Идея и срутктура программы такая же, как в задаче 1.

## Задание 3

**[SAVE] Результат всех вышеперечисленных шагов сохранить в репозиторий (+ отчет по данной ЛР в папку doc)**

Фиксацию ревизий производить строго через ветку `dev`. С помощью скриптов
накатить ревизии на `stg` и на `prd`. Скрипты разместить в корне репозитория. Также
создать скрипты по возврату к виду текущей ревизии (даже если в папке имеются
несохраненные изменения + новые файлы).

- Скрипт для подтягивания коммитов с ветки `dev` на ветку `stg` - [`dev2stg.sh`](../../dev2stg.sh)
- скрипт для подтягивания коммитов с ветки `stg` на ветку `prd` - [`stg2prd.sh`](../../stg2prd.sh)
- Скрипт для отката изменений в текущей ревизии - [`reset.sh`](../../reset.sh)

Полезная [ссылка](https://graphite.dev/guides/git-discard-changes).

## Вывод
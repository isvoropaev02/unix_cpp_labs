# ЛР #1: [ALGO]: Sorting algorithms

Студент: Воропаев И. С.

Группа: Z4200

Преподаватель: Маслов И. Д.

Год: 2025

## Цель

Познакомиться с основами анализа алгоритмов на примере операций сортировки. Алгоритмы на выбор:

- сортировка пузырьком (Bubble sort)
- сортировка выбором (Selection sort)
- сортировка вставкой (Insertion sort)
- сортировка подсчетом (Counting sort)
- цифровая сортировка (LSD, MSD)
- сортировка слиянием (Merge sort)
- шейкерная сортировка (Cocktail sort)
- быстрая сортировка / сортировка Хоара (Quicksort)
- сортировка кучей / пирамидальная (Heap sort)

## Задание 1

- Сформулировать задачу, для которой требуется применение алгоритма сортировки.
- Выбрать алгоритм сортировки.
- Доказать, что выбранный алгоритм является наилучшим выбором. Привести расчет ёмкостных и вычислительных затрат.
- Реализовать алгоритм на языке C++.

### Задача:

Дан массив из случайных чисел `(int, float)`. Необходимо отсортировать этот массив по возрастанию.

### Выбор алгоритма, сложность, память:

Так как о распределении исходных данных нет априорной информации, желательно взять алгоритм, который одинкаово хорошо работает в ситуциях, когда массив близок к отсортированному по возрастанию/убыванию и в промежуточных случаях.

Таким алгоритмом может являться `quick-sort`. Средняя асимптотика его сложности равна $O(n\cdot \log n)$, где $n$ - размер массива (показано ниже).

**Оценим сложность алгоритма по мастер-теореме:**

Обозначения: $T(n)$ -время, требующееся алгоритму для сортировки $n$ элементов, $M(n)$ - память затраченная алгоритмом на сортировку $n$ элементов.

Так как сортировка происходит *in-place*, дополнительные затраты включают только память на хранения стека рекурсивных вызовов. Поэтому $M$ также будет показывать глубину рекурсии.

1. В *среднем* и, заодно, *лучшем* случае массив на каждом шаге алгоритма будет делиться пополам. Разбиение массива размера $n$ на 2 части (операция `partition`) требует $n$ сравнений, значит имеет асимптотику $O(n)$. Алгоритм вызывает самого себя 2 $(O(1))$ раза на каждом шаге. Тогда:

$$T(n)=2T(n/2)+O(n)$$

$$M(n)=2M(n/2)+O(1)$$

Отсюда (пользуясь [этим вариантом теоремы](https://ru.wikipedia.org/wiki/%D0%9E%D1%81%D0%BD%D0%BE%D0%B2%D0%BD%D0%B0%D1%8F_%D1%82%D0%B5%D0%BE%D1%80%D0%B5%D0%BC%D0%B0_%D0%BE_%D1%80%D0%B5%D0%BA%D1%83%D1%80%D1%80%D0%B5%D0%BD%D1%82%D0%BD%D1%8B%D1%85_%D1%81%D0%BE%D0%BE%D1%82%D0%BD%D0%BE%D1%88%D0%B5%D0%BD%D0%B8%D1%8F%D1%85#:~:text=2%5B%D0%BF%D1%80%D0%B0%D0%B2%D0%B8%D1%82%D1%8C%20%7C%20%D0%BF%D1%80%D0%B0%D0%B2%D0%B8%D1%82%D1%8C%20%D0%BA%D0%BE%D0%B4%5D-,%D0%9E%D0%B1%D1%89%D0%B0%D1%8F%20%D1%84%D0%BE%D1%80%D0%BC%D0%B0,-%5B%D0%BF%D1%80%D0%B0%D0%B2%D0%B8%D1%82%D1%8C%20%7C%20%D0%BF%D1%80%D0%B0%D0%B2%D0%B8%D1%82%D1%8C%20%D0%BA%D0%BE%D0%B4%5D)):

$$a=2, b=2, c=1, c=\log_b a \quad \Rightarrow \quad T(n)=O(n\log n)$$

$$a=2, b=2, c=0, c=\log_b a \quad \Rightarrow \quad M(n)=O(\log n)$$

2. В худшем случае на каждом шаге алоритма массив будет делиться на 2 неравных, в одном из которых будет мало элементов (например, $1$), а в другом будут почти все оставшиеся, (например $n-1$). На каждом шаге алгоритм совершает 2 вызова ($O(1)$), один из которых в дальнейшем не развивает рекурсию. Тогда рекуррентное выражение будет иметь вид:

$$T(n)=T(n-1)+O(n)$$

$$M(n)=M(n-1)+O(1)$$

Выражение для $T$ задает алгебраическую прогрессию, поэтому из формулы для суммы алгебраической прогрессии $T(n)=O(n^2)$.

Решение рекуррентного выражения для $M$ дает сумму единиц, значит $M(n)=O(n)$.

### Реализация:

Реализацию алгоритма можно увидеть в файле [`../src/quick_sort.h`](../src/quick_sort.h)

```
./lab6/scripts/build_project.sh   # компиляция
lab6/build/main                   # запуск
```

## Задание 2

- Сформулировать задачу, для которой требуется применение алгоритма сортировки. Алгоритм сортировки 2 должен отличаться от алгоритма сортировки 1. Асимтотическая сложность алгоритма 2 должна быть больше, чем у алгоритма 1.
- Выбрать алгоритм сортировки. Доказать, что выбранный алгоритм является наилучшим выбором. Привести расчет ёмкостных и вычислительных затрат.
- Реализовать алгоритм на языке C++.

### Задача:

Пусть теперь дан массив натуральных чисел не сильно большого размера, который "почти" отсортирован или действительно отсортирован. Необходимо его отсортировать на выходе. (Необходим алгоритм, для которого минимум сложности достигается, когда массив изначально отсортирован)

### Выбор алгоритма, сложность, память:

так как заранее известно, что в большинстве случаев массив будет близок к отсортированному, нужно выбрать алгоритм, который имеет наименьшую сложность для отсортированного массива. Поэтому был выбран алгоритм `cocktail-sort` (модификация алгоритма `bubble-sort`). Он имеет наименьшую сложность при почти отсортированных массивах - $O(n)$ (показано ниже), при этом не требует дополнительных затрат памяти (показано ниже). Однако в среднем он сложнее `quick-sort`.

**Оценим сложность алгоритма:**

Обозначения: $T(n)$ -время, требующееся алгоритму для сортировки $n$ элементов, $M(n)$ - память затраченная алгоритмом на сортировку $n$ элементов.

Заранее можно сказать, что как и `bubble-sort` этот алгоритм не требует дополнительной памяти и $M(n)=O(1)$ всегда.

1. В худшем и среднем случае, алгоритм на каждом следующем шаге будет проходить подмассив размера на 1 меньше, пока не достигнет подмассива из 1 элемента. Поэтому сложность можно записать в виде:

$$T(n)=T(n-1)+O(n)$$

Это задает сумму алгебраической прогрессии. Поэтому: $T(n)=O(n^2)$

2. В лучшем случае (как раз случай, соответствующий данной задаче), когда массив очень близок к отсортированному, алгоритм остановится на одном из первых проходов, сделав на каждом из них около $n$ сравнений:

$$T(n)=O(n)$$

### Реализация:

Реализацию алгоритма можно увидеть в файле [`../src/cocktail_sort.h`](../src/cocktail_sort.h)

```
./lab6/scripts/build_project.sh   # компиляция
lab6/build/main                   # запуск
```

## Результаты:

Тестовые сценарии генерировались с помощью скрипта: [`../src/generate_tests.py`](../src/generate_tests.py).

### Сравнение времени выполнения для 1 задачи:

Для 1 задачи генерируется набор случайных чисел в плавающей точке (`float32`) заданного размера, равномерно распределенных на интервале $[-1, 1]$.

|Размер массива (`float32`) |Время (`Quick-sort`) |Время (`Cocktail-sort`) |
|:--------------------------|:--------------------|:-----------------------|
| 128                       | 24 мкс              | 146 мкс                |
| 512                       | 56 мкс              | 1145 мкс               |
| 1024                      | 127 мкс             | 4330 мкс               |
| 4096                      | 570 мкс             | 69120 мкс              |
| 10000                     | 1570 мкс            | 430368 мкс             |

### Сравнение времени выполнения для 2 задачи:

Для 2 задачи генерируется массив `range(0,n,1)` типа `int32`, при этом делается так, чтоб $2$% элемпентов в нем поменялись друг с другом местами.

|Размер массива (`int32`)   |Время (`Quick-sort`) |Время (`Cocktail-sort`) |
|:--------------------------|:--------------------|:-----------------------|
| 128                       | 97 мкс              | 5 мкс                  |
| 512                       | 670 мкс             | 42 мкс                 |
| 1024                      | 1470 мкс            | 115 мкс                |
| 4096                      | 1920 мкс            | 2250 мкс               |
| 10000                     | 9040 мкс            | 17500 мкс              |

### Вывод

- Были реализованы алгоритмы `Quick-sort` и `Cocktail-sort`. Их работа в 2 разных сценариях при разных размерах массива приведена выше в таблицах.

- В большинстве случаев, когда массив не близок к отсортированному или когда массив очень большой, алгоритм `Quick-sort` справляется быстрее.

- На большом почти отсортированном массиве алгоритм `Cocktail-sort` проиграл из-за того, что в нем помимо операций сравнения делается на каждом шаге операция `swap`, которая сильно затратная. Плюс, учитывая, что 2% массива требовало сортировки, количество необходимых алгоритму проходов росло, что приближало его сложность к $n^2$ с ростом $n$.

- Из таблиц видно, что время одного сравнения между `float32` числами **больше** времени сравнения между `int32` числами при равном числе бит.

- Выбор алгоритма `Cocktail-sort` для янужд задачи 2 (первые 3 строчки второй таблицы) оправдал себя и дал выигрышь у `Quick-sort`. Такой сценарий может быть, например, в каком-то протоколе сети, который перераспределяет принятые пакеты по их номеру. Пакеты почти идут подряд, но иногда случаются перестановки.